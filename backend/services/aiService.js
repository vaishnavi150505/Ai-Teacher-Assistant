export const analyzeAssignment = async (filePath) => {
  // Simulate AI grading logic
  return {
    score: Math.floor(Math.random() * 10) + 1, // Random score between 1 and 10
    comments: "Great work! Keep improving on your analysis.",
  };
};
